# Replace <CONVEX_HOST> with the IP or hostname where the Convex Dockge stack runs.
# This URL is sent to clients' browsers, so it must be reachable from their network.
NUXT_PUBLIC_CONVEX_URL=http://<CONVEX_HOST>:3210
