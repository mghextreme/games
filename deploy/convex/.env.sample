# Replace <CONVEX_HOST> with the IP or hostname where this Dockge stack runs.
# This must be reachable from both the frontend LXC and from clients' browsers
# (since browsers connect to Convex directly via WebSocket).
CONVEX_URL=http://<CONVEX_HOST>:3210
CONVEX_SITE_URL=http://<CONVEX_HOST>:3211
